// Placeholder — Step 8 fills this in with the listings grid and the four
// data states (loading/error/empty/populated). No fetch here yet.
function BrowsePage() {
  return <h1 className="text-2xl font-semibold text-zinc-900">Browse listings</h1>
}

export default BrowsePage
