// Every contact and rights-request line in both legal documents reads this,
// so it is the only place an address changes. It is compiled into the bundle
// at build time, not read from the environment — changing it needs a rebuild.
export const CONTACT_EMAIL = 'luismgon4@gmail.com'

export const GOVERNING_LAW = 'the State of California, United States'

export const SERVICE_NAME = 'Card Marketplace'

// The date both documents display. Bump it whenever their rendered text
// changes — the constants above are part of that text, not configuration.
export const LAST_UPDATED = 'August 12, 2026'
